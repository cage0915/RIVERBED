1. Use icon for confirm button, same as confirm button in the caption popup.
2. Integrate new-album.js into dev tool. in folder page, when hitting the gear, popup a folder manager. It can reorder and create a new album. For reorder, it can drag and drop like reorder layer1 in page manager. And find a way to store the order instead of recording the order in the album .mdx like currently way. For create new album, user can browse from the folders under corresponding folder. like if in <blog>/yama will look for folders under content/albums/yama. (So user need to prepare the photo folder first). Refer to new-album.js, but now just process the one user choose, not trying over all folders.
